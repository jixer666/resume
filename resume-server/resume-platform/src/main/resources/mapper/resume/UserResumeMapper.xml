<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.abc.resume.resume.mapper.UserResumeMapper">

    <resultMap type="com.abc.resume.resume.domain.entity.UserResume" id="UserResumeResult">
        <result property="id"    column="id"    />
        <result property="uid"    column="uid"    />
        <result property="resumeDetail"    column="resume_detail"    typeHandler="com.abc.resume.resume.domain.entity.resume.UserResumeDetailTypeHandler"    />
        <result property="thumbnail"    column="thumbnail"    />
        <result property="templateCode"    column="template_code"    />
        <result property="createTime"    column="create_time"    />
        <result property="status"    column="status"    />
        <result property="updateTime"    column="update_time"    />
        <result property="ver"    column="ver"    />
        <result property="isDelete"    column="is_delete"    />
    </resultMap>

    <sql id="selectUserResumeVo">
        select id, uid, resume_detail, thumbnail, template_code, create_time, status, update_time, ver, is_delete from user_resume
    </sql>

    <select id="selectUserResumePage" resultMap="UserResumeResult">
        <include refid="selectUserResumeVo"/>
        where uid = #{uid} and is_delete != id
        order by update_time desc, id desc
    </select>

    <select id="selectUserResumeById" resultMap="UserResumeResult">
        <include refid="selectUserResumeVo"/>
        where id = #{id} and uid = #{uid} and is_delete != id
    </select>

    <insert id="insertUserResume" useGeneratedKeys="true" keyProperty="id">
        insert into user_resume (uid, resume_detail, thumbnail, template_code, create_time, status, update_time, ver, is_delete)
        values (#{uid}, #{resumeDetail, typeHandler=com.abc.resume.resume.domain.entity.resume.UserResumeDetailTypeHandler}, #{thumbnail}, #{templateCode}, #{createTime}, #{status}, #{updateTime}, #{ver}, #{isDelete})
    </insert>

    <update id="updateUserResume">
        update user_resume
        set resume_detail = #{resumeDetail, typeHandler=com.abc.resume.resume.domain.entity.resume.UserResumeDetailTypeHandler},
            thumbnail = #{thumbnail},
            template_code = #{templateCode},
            update_time = #{updateTime},
            ver = #{ver}
        where id = #{id} and uid = #{uid} and is_delete != id
    </update>

    <update id="deleteUserResume">
        update user_resume
        set is_delete = id,
            update_time = now(),
            ver = ver + 1
        where id = #{id} and is_delete != id
    </update>

</mapper>
